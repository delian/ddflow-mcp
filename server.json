{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.delian/ddflow-mcp",
  "title": "ddflow",
  "description": "Work-queue kernel for AI coding agents: phases and tasks with dependencies, parallel git-worktree isolation, crash recovery, a quality-gate pipeline, and cross-family review. The event log is the source of truth, so the project rebuilds from it alone.",
  "repository": {
    "url": "https://github.com/delian/ddflow-mcp",
    "source": "github"
  },
  "version": "0.1.0",
  "packages": [
    {
      "registryType": "pypi",
      "registryBaseUrl": "https://pypi.org",
      "identifier": "ddflow-mcp",
      "version": "0.1.0",
      "runtimeHint": "uvx",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "DDFLOW_REPO",
          "description": "Repository root ddflow manages. Defaults to the client's working directory, resolved to the primary checkout even when called from inside a git worktree.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "DDFLOW_AGENT",
          "description": "Stable identity for this agent. Shards the event log so concurrent agents never write the same file. Defaults to host plus the git worktree's name, so every process in one worktree agrees and two parallel agents differ. A harness running SEVERAL agents inside one worktree must set this, or their work merges into a single identity.",
          "isRequired": false,
          "isSecret": false
        }
      ]
    },
    {
      "registryType": "oci",
      "identifier": "docker.io/delian/ddflow-mcp:0.1.0",
      "version": "0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "DDFLOW_REPO",
          "description": "Repository root ddflow manages. Defaults to the client's working directory, resolved to the primary checkout even when called from inside a git worktree.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "DDFLOW_AGENT",
          "description": "Stable identity for this agent. Shards the event log so concurrent agents never write the same file. Defaults to host plus the git worktree's name, so every process in one worktree agrees and two parallel agents differ. A harness running SEVERAL agents inside one worktree must set this, or their work merges into a single identity.",
          "isRequired": false,
          "isSecret": false
        }
      ]
    },
    {
      "registryType": "oci",
      "identifier": "ghcr.io/delian/ddflow-mcp:0.1.0",
      "version": "0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "DDFLOW_REPO",
          "description": "Repository root ddflow manages. Defaults to the client's working directory, resolved to the primary checkout even when called from inside a git worktree.",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "DDFLOW_AGENT",
          "description": "Stable identity for this agent. Shards the event log so concurrent agents never write the same file. Defaults to host plus the git worktree's name, so every process in one worktree agrees and two parallel agents differ. A harness running SEVERAL agents inside one worktree must set this, or their work merges into a single identity.",
          "isRequired": false,
          "isSecret": false
        }
      ]
    }
  ]
}
